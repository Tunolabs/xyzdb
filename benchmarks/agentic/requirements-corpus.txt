# Extra dependencies to BUILD the corpus embeddings (build_lme.py only). Heavy: ~2 GB of
# wheels plus the ~1.3 GB bge-large model download. NOT needed if you rsync a prebuilt
# corpora/lme/ (cvec.npy, qvec.npy, meta.json). Install into a dedicated venv.
#
# RECALL IS EMBEDDER-VERSION-SENSITIVE, so the version is PINNED here to keep the auto-build
# deterministic: everyone who builds the corpus from source gets the same vectors. sentence-
# transformers 5.1.2 yields academic LongMemEval recall ~0.91 on this engine; the older 2.2.2
# yielded ~0.96 — an embedder change, not an engine regression. (The published matrix tables
# report recall = 1.00: that is exact NEAREST vs the oracle top-k, which the embedder does not
# change; the ~0.9x figure is the separate academic-recall checkpoint.)
#
# To reproduce the exact vectors of an EARLIER run instead of rebuilding, drop that run's
# prebuilt corpora/lme/ in place — build_lme.py (and the runner's auto-provision) is skipped
# when the corpus is already present.
#
# Model: BAAI/bge-large-en-v1.5 (1024-d), pulled from Hugging Face on first run.
sentence-transformers==5.1.2
torch>=2.2
